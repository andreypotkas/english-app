# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=english_app
# true = TypeORM создаёт/обновляет таблицы при старте (для сервера без миграций)
DB_SYNCHRONIZE=true

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES=7d

# Optional
NODE_ENV=development
